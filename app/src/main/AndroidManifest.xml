<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="br.com.solucoes161.java.smscarmanager" >

    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.READ_SMS" />
    <uses-permission android:name="android.permission.WRITE_SMS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:replace="android:icon,android:theme" >
        <activity
            android:name="io.github.salemlockwood.android.smscarmanager.MainActivity"
            android:label="@string/title_activity_main"
            android:launchMode="singleTop"
            android:theme="@style/AppTheme.NoActionBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="io.github.salemlockwood.android.smscarmanager.CommandsSettingsActivity" />

        <receiver
            android:name="io.github.salemlockwood.android.smscarmanager.SmsReceiver"
            android:enabled="true"
            android:exported="true" >
            <intent-filter android:priority="999" >
                <action android:name="android.provider.Telephony.SMS_RECEIVED" />
            </intent-filter>
        </receiver>

        <activity android:name="io.github.salemlockwood.android.smscarmanager.ConfigActivity" />
        <activity
            android:name="io.github.salemlockwood.android.smscarmanager.PhonesListActivity"
            android:label="@string/title_activity_phones_list"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name="io.github.salemlockwood.android.smscarmanager.PhonesInsertActivity"
            android:label="@string/title_activity_phones_insert"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name="io.github.salemlockwood.android.smscarmanager.AlertDialogActivity"
            android:label="Alerta"
            android:theme="@style/AppTheme.NoActionBar" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name="io.github.salemlockwood.android.smscarmanager.MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity
            android:name="io.github.salemlockwood.android.smscarmanager.MapTrackActivity"
            android:label="@string/title_activity_map_track" >
        </activity>
        <activity android:name="io.github.salemlockwood.android.smscarmanager.TracksActivity" >
        </activity>
    </application>

</manifest>
